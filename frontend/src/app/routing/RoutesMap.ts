import {CustomRoutesMap} from "./customNavbar";


const RoutesMap = {
    HOME: '/',
    RESPONSE: '/response',
    CONFIG: '/config',
    SYSTEM: '/system',
    ...CustomRoutesMap
};

export default RoutesMap
